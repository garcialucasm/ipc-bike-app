export interface User {
    id: number;
    name: string;
    // room: string;
    // currentStatus?: string;
    // availability?: string;
    // usageCount?: number;
    // createdAt?: string;
    // updatedAt?: string;
  }